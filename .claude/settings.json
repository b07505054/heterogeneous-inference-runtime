{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) cd /Users/allen/Documents/Codex/project/heterogeneous-inference-runtime && .venv/bin/python -m pytest agentic_eval/tests tests -q ;; esac; } 2>&1 || true",
            "statusMessage": "Running pytest for validation..."
          }
        ]
      }
    ]
  }
}
