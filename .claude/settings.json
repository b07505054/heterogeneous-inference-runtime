{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) cd /Users/allen/Documents/Codex/project/heterogeneous-inference-runtime && bash scripts/check.sh ;; esac; } 2>&1 || true",
            "statusMessage": "Running pytest for validation..."
          }
        ]
      }
    ]
  }
}
